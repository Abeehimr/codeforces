{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Python",
            "type": "shell",
            "command": "python3 ${relativeFile} < input.txt > output.txt",
            "presentation": { "reveal": "silent" },
            "group": { "kind": "test", "isDefault": true }
        },
        {
        "label": "C++ (Debug) Build+Run",
        "type": "shell",
        "command": "g++ ${file} -std=c++17 -O0 -g -pipe -Wall -Wextra -Wshadow -fsanitize=address,undefined -DLOCAL -o ${fileBasenameNoExtension}.out && ./${fileBasenameNoExtension}.out < input.txt > output.txt",
        "presentation": { "reveal": "silent" },
        "group": { "kind": "test", "isDefault": true }
        }
    ]
}