#include <bits/stdc++.h>
using namespace std;

#define int long long
#define rep(i, a, b) for(int i = a; i < (b); ++i)
#define all(x) begin(x), end(x)
#define len(x) (int)(x).size()
#define ph push_back
#define pp pop_back
#define ln cout << endl
#define bitcount(x) (__builtin_popcountll((unsigned long long)(x)))
#define bitlength(x) ((x) ? 64 - __builtin_clzll((unsigned long long)(x)) : 0)

template<typename T>
using heap = priority_queue<T, vector<T>, greater<T>>;

typedef pair<int, int> pii;
typedef vector<int> vi;
typedef vector<bool> vb;
#define MOD 1000000007LL
#define MOD1 998244353LL
void solve(){}
signed main() {
    cin.tie(0)->sync_with_stdio(0);
    //cin.exceptions(cin.failbit);

    int q = 1;
    //cin >> q;
    while (q--) solve();
}